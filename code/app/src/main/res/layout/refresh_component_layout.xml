<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">

    <TextView
        android:id="@+id/tvClickToRefresh"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="@dimen/margin_6dp"
        android:layout_marginLeft="@dimen/margin_6dp"
        android:layout_marginTop="@dimen/margin_6dp"
        android:padding="@dimen/padding_10dp"
        android:textSize="@dimen/sub_header_text_size"
        android:text="@string/click_to_refresh"
        android:textStyle="bold"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        />

    <ImageView
        android:id="@+id/ivRefresh"
        android:layout_width="@dimen/iv_refresh_width"
        android:layout_height="@dimen/iv_refresh_height"
        android:layout_marginRight="@dimen/margin_8dp"
        android:layout_marginEnd="@dimen/margin_8dp"
        android:background="@drawable/ic_refresh"
        android:padding="@dimen/padding_10dp"
        app:layout_constraintBottom_toBottomOf="@+id/tvClickToRefresh"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="@+id/tvClickToRefresh"
        />

</androidx.constraintlayout.widget.ConstraintLayout>